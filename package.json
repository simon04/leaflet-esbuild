{
  "name": "leaflet-esbuild",
  "private": true,
  "license": "ISC",
  "scripts": {
    "build": "esbuild --bundle --outdir=dist/ --loader:.png=file index.js"
  },
  "dependencies": {
    "leaflet": "^1.8.0-beta.1"
  },
  "devDependencies": {
    "@types/leaflet": "^1.7.9",
    "esbuild": "^0.14.25"
  }
}
