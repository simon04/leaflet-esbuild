{
  "name": "leaflet-esbuild",
  "private": true,
  "license": "ISC",
  "scripts": {
    "build": "esbuild --bundle --outdir=dist/ --loader:.png=file index.js"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.3",
    "esbuild": "^0.25.2"
  }
}
